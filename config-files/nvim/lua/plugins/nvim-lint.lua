local util = require("core.util")
local lint = require("lint")
-- lint.linters_by_ft = util.get_language_linters()
--
-- Linters are always a pain to deal with IMO because it always requires some
-- setup involved. For me lsp's + formatter is enough
-- Style guides?
-- just format my shit correctly dawg
